
# Configurações de Ambiente para Produção
# Copie este arquivo para .env e ajuste os valores conforme necessário

# === CONFIGURAÇÕES BÁSICAS ===
NODE_ENV=production
DOMAIN=seu-dominio.com
BASE_PATH=/

# === URLs DA APLICAÇÃO ===
VITE_API_URL=https://seu-dominio.com/api
VITE_BASE_PATH=/
VITE_PUBLIC_URL=https://seu-dominio.com

# === AUTENTICAÇÃO ===
# Senhas dos usuários padrão (altere estas senhas!)
ADMIN_PASSWORD=sua-senha-admin-segura
USER_PASSWORD=sua-senha-user-segura

# Chave secreta para JWT (gere uma chave aleatória segura)
JWT_SECRET=sua-chave-jwt-super-secreta-aqui

# === CONFIGURAÇÕES DE CORS ===
CORS_ORIGIN=*

# === CAMINHOS DOS VOLUMES (para mapeamento no host) ===
DATA_PATH=./data
SCRIPTS_PATH=./scripts
LOGS_PATH=./logs
UPLOADS_PATH=./uploads

# === CONFIGURAÇÕES DE SSL (opcional) ===
SSL_CERT_PATH=./ssl/cert.pem
SSL_KEY_PATH=./ssl/key.pem

# === CONFIGURAÇÕES DE BACKUP (opcional) ===
BACKUP_PATH=./backups
BACKUP_SCHEDULE=0 2 * * *

# === EXEMPLO PARA SUBPATH ===
# Para usar em um subpath como https://dominio.com/scripts/
# DOMAIN=dominio.com
# BASE_PATH=/scripts
# VITE_API_URL=https://dominio.com/scripts/api
# VITE_BASE_PATH=/scripts
# VITE_PUBLIC_URL=https://dominio.com/scripts

# === CONFIGURAÇÕES DO PORTAINER STACK ===
# Use estas variáveis ao criar o stack no Portainer:
# - DOMAIN
# - BASE_PATH  
# - ADMIN_PASSWORD
# - USER_PASSWORD
# - JWT_SECRET
# - DATA_PATH (caminho no host onde os dados serão persistidos)
# - SCRIPTS_PATH (caminho no host onde os scripts serão armazenados)
